import { createIconSetFromFontello } from '@expo/vector-icons';
import fontelloConfig from '../config/font-config.json';

const Icon = createIconSetFromFontello(fontelloConfig, 'Icons');

export default Icon;
