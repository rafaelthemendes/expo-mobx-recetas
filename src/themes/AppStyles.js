import Colors from './Colors';

// Reusable styles
const AppStyles = {
  screen: {
    mainScreen: {
      flex: 1,
      backgroundColor: Colors.mainBackground
    }
  }
};

export default AppStyles;
